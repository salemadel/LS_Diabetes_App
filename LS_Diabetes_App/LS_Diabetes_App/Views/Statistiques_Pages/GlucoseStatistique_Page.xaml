<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Custom="clr-namespace:LS_Diabetes_App.Custom_Controls"
             xmlns:chart="clr-namespace:Syncfusion.SfChart.XForms;assembly=Syncfusion.SfChart.XForms"
             xmlns:Converters="clr-namespace:LS_Diabetes_App.Converters"
             mc:Ignorable="d"
             x:Class="LS_Diabetes_App.Views.Statistiques_Pages.GlucoseStatistique_Page">
    <ContentPage.Resources>
        <ResourceDictionary>
            <OnPlatform x:TypeArguments="x:String" x:Key="NormalFont">
                <On Platform="Android" Value="VarelaRound-Regular.ttf#Open Sans" />
            </OnPlatform>
            <OnPlatform x:TypeArguments="x:String" x:Key="BoldFont">
                <On Platform="Android" Value="Roboto-Bold.ttf#Open Sans" />
            </OnPlatform>
            <OnPlatform x:TypeArguments="x:String" x:Key="ExtraBoldFont">
                <On Platform="Android" Value="OpenSans-ExtraBold.ttf#Open Sans" />
            </OnPlatform>
            <Converters:ZeroToDashConverter x:Key="ZeroConverter" />
            <chart:ChartColorCollection x:Key="Colors">
                <Color>#0AC774</Color>
                <Color>#e74c3c</Color>
                <Color>#f1c40f</Color>
            </chart:ChartColorCollection>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ScrollView>
        <Grid RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="120" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" BackgroundColor="Default">
                <Label Text="Statistique Glycemie." FontSize="30" Margin="10,10,10,10" FontFamily="{StaticResource ExtraBoldFont}" FontAttributes="Bold" TextColor="Black" HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand" />
            </StackLayout>
            <Grid Grid.Row="1" VerticalOptions="StartAndExpand">
                <Grid.RowDefinitions>
                    <RowDefinition Height="80" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="20" />
                </Grid.RowDefinitions>
                <StackLayout Grid.Row="0" Orientation="Horizontal" Margin="10,0,0,0">
                    <Custom:Cusom_DatePicker Grid.Column="0" Date="{Binding Selected_MinDate}" FontFamily="{StaticResource NormalFont}" FontSize="16" Format="dd/MM/yyyy" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" />
                    <Custom:Cusom_DatePicker Grid.Column="1" Date="{Binding Selected_MaxDate}" FontFamily="{StaticResource NormalFont}" FontSize="16" Format="dd/MM/yyyy" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" Margin="10,0,0,0" />
                    <ImageButton Grid.Column="2" HeightRequest="40" WidthRequest="40" CornerRadius="40" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" Margin="0,0,0,0" BackgroundColor="#00B2C8" Source="next.png" Padding="10" Command="{Binding FiltreCommand}" />
                </StackLayout>
                <Frame Grid.Row="1" Margin="5,5,5,5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="240" />
                            <RowDefinition Height="160" />
                            <RowDefinition Height="160" />
                            <RowDefinition Height="290" />
                        </Grid.RowDefinitions>
                        <chart:SfChart Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"  VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" HeightRequest="400" WidthRequest="400">
                            <chart:SfChart.Series>
                                <chart:DoughnutSeries ItemsSource="{Binding Slices}" XBindingPath="type" YBindingPath="value" DoughnutCoefficient="0.85">
                                   
                                    <chart:DoughnutSeries.ColorModel>
                                        <chart:ChartColorModel Palette="Custom" CustomBrushes="{StaticResource Colors}"/>
                                    </chart:DoughnutSeries.ColorModel>
                                </chart:DoughnutSeries>
                            </chart:SfChart.Series>
                        </chart:SfChart>
                        <StackLayout Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
                            <Label Text="Moyenne" FontFamily="{StaticResource NormalFont}" FontSize="14" TextColor="{Binding GlucoseColor}" HorizontalOptions="CenterAndExpand" Margin="0,5,0,0" />
                            <Label Text="{Binding Average , Converter={StaticResource ZeroConverter}}" TextColor="{Binding GlucoseColor}" FontFamily="{StaticResource NormalFont}" FontSize="24" HorizontalOptions="CenterAndExpand" Margin="0,5,0,0" />
                            <Label Text="{Binding Profil.GlycemiaUnit}" TextColor="{Binding GlucoseColor}" FontFamily="{StaticResource NormalFont}" FontSize="13" HorizontalOptions="CenterAndExpand" Margin="0,5,0,0" />
                        </StackLayout>
                        <StackLayout Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" HeightRequest="155" WidthRequest="300" Margin="5,10,5,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackLayout Grid.Row="0" Grid.Column="0">
                                    <Label Text="Bonne" FontFamily="{StaticResource NormalFont}" FontSize="14" TextColor="#0AC774" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                    <Label Text="{Binding Nbr_Normal , Converter={StaticResource ZeroConverter}}" TextColor="#0AC774" FontFamily="{StaticResource NormalFont}" FontSize="20" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                </StackLayout>
                                <StackLayout Grid.Row="0" Grid.Column="1">
                                    <Label Text="Elevée" FontFamily="{StaticResource NormalFont}" FontSize="14" TextColor="#e74c3c" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                    <Label Text="{Binding Nbr_Hight , Converter={StaticResource ZeroConverter}}" TextColor="#e74c3c" FontFamily="{StaticResource NormalFont}" FontSize="20" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                </StackLayout>
                                <StackLayout Grid.Row="0" Grid.Column="2">
                                    <Label Text="Basse" FontFamily="{StaticResource NormalFont}" FontSize="14" TextColor="#f1c40f" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                    <Label Text="{Binding Nbr_Low , Converter={StaticResource ZeroConverter}}" TextColor="#f1c40f" FontFamily="{StaticResource NormalFont}" FontSize="20" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                </StackLayout>
                            </Grid>
                        </StackLayout>
                        <StackLayout Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" HeightRequest="155" WidthRequest="300" Margin="5,10,5,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackLayout  Grid.Column="0">
                                    <Label Text="La Plus haute" FontFamily="{StaticResource NormalFont}" FontSize="14" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                    <Label Text="{Binding Max.Glycemia , Converter={StaticResource ZeroConverter}, FallbackValue='--'}" TextColor="Black" FontFamily="{StaticResource NormalFont}" FontSize="20" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                    <Label Text="{Binding Max.Date , StringFormat='{0:dd/MM/yyyy HH:mm}', FallbackValue='--'}"  FontFamily="{StaticResource NormalFont}" FontSize="13" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                </StackLayout>
                                <StackLayout Grid.Column="1">
                                    <Label Text="La Plus Basse" FontFamily="{StaticResource NormalFont}" FontSize="14" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                    <Label Text="{Binding Min.Glycemia , Converter={StaticResource ZeroConverter} , FallbackValue='--'}" TextColor="Black" FontFamily="{StaticResource NormalFont}" FontSize="20" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                    <Label Text="{Binding Min.Date , StringFormat='{0:dd/MM/yyyy HH:mm}', FallbackValue='--'}"  FontFamily="{StaticResource NormalFont}" FontSize="13" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0" />
                                </StackLayout>
                            </Grid>
                        </StackLayout>

                        <chart:SfChart Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" HeightRequest="280" WidthRequest="300" Margin="5,10,5,0">

                            <chart:SfChart.PrimaryAxis>

                                <chart:DateTimeCategoryAxis Interval="1" IntervalType="Days">
                                </chart:DateTimeCategoryAxis>
                            </chart:SfChart.PrimaryAxis>

                            <chart:SfChart.SecondaryAxis>

                                <chart:NumericalAxis Minimum="0" Maximum="{Binding MaximumChart}" ShowMajorGridLines="False" ShowMinorGridLines="False">
                                </chart:NumericalAxis>
                            </chart:SfChart.SecondaryAxis>
                            <chart:LineSeries ItemsSource="{Binding Glucose_Data}" XBindingPath="Date" YBindingPath="Glycemia" Color="{Binding GlucoseColor}" StrokeWidth="5">
                            </chart:LineSeries>
                        </chart:SfChart>
                    </Grid>
                </Frame>

                <Frame Grid.Row="2" VerticalOptions="EndAndExpand"  HorizontalOptions="CenterAndExpand" IsVisible="{Binding IsBusy}" HeightRequest="45" WidthRequest="45" CornerRadius="50" Padding="0" BackgroundColor="White" Margin="0,0,0,10">
                    <ActivityIndicator  Color="#275db5"  VerticalOptions="CenterAndExpand"  HorizontalOptions="CenterAndExpand" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" HeightRequest="40" WidthRequest="40" />
                </Frame>
            </Grid>
        </Grid>
    </ScrollView>
</ContentPage>